<?xml version="1.0"?>


<odoo>
    <record id="retour_bons_correction" model="ir.ui.view">
        <field name="name">base.group.merge.automatic.wizard.tree</field>
        <field name="model">base.group.merge.automatic.wizard</field>
        <field name="arch" type="xml">
            <tree string="Groups" create="false" edit="false" delete="false">

                <field name="date_start_r" string="Date"/>
                <field name="project_id" string="Projet"/>
                <field name="zo" string="Zone"/>
                <field name="sect" string="Secteur"/>
                <field name="gest_id" string="Superviseur"/>
                <field name="coordin_id" string="Coordinateur"/>
                <field name="employee_id" string="Intervenant"/>
                <field name="gest_id2" string="Affect. Controle"/>
                <field name="emp_id2" string="Affect. Correction"/>
                <field name="state" string="Etat Bon" invisible="1"/>
                <field name="state1" string="Etat Controle" invisible="1"/>
                <field name="state2" string="Etat Correction"/>
                <button name="action_open2" string="Valider/Annuler bons" type="object" icon="fa-pencil"/>

            </tree>
        </field>

    </record>
</odoo>